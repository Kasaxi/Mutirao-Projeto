node_modules/
dist/
target/
src-tauri/target/
*.log
.DS_Store

# Segredos. A chave da API nunca entra no repositório: o adaptador a lê do
# ambiente. Um repositório privado não é cofre — quem clona, leva.
.env
.env.*
!.env.exemplo
*.pem
*.key

# Banco local de desenvolvimento. O de verdade fica em %APPDATA%.
*.db
*.db-shm
*.db-wal
